// Thin wrapper to reuse the canonical icon definitions from constants.ts
import constants, { EMPTY_TALISMAN_ICON, getDefaultIcon } from './constants'

export { EMPTY_TALISMAN_ICON, getDefaultIcon }

export default {
  EMPTY_TALISMAN_ICON,
  getDefaultIcon,
}
